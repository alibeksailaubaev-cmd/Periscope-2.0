# Periscope 2.0 — камера фиксации людей в очках
Flask>=3.0.0

# Компьютерное зрение.
# На сервере без графической оболочки замените на opencv-python-headless.
opencv-python>=4.8.0

# Детекция лиц и ключевых точек (MediaPipe Tasks / Solutions).
# Если mediapipe не установится (например, нет колеса под вашу версию Python),
# приложение автоматически переключится на каскады Хаара из OpenCV.
mediapipe>=0.10.0

numpy>=1.24.0
Pillow>=10.0.0

# Отчёты: CSV/JSON работают без pandas, XLSX-экспорт — с ним.
pandas>=2.0.0
openpyxl>=3.1.0
